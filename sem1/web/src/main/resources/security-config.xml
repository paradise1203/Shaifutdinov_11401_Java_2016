<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <security:http auto-config="true" use-expressions="true" access-denied-page="/auth/login">
        <security:intercept-url pattern="/auth/login" access="isAnonymous()"/>
        <security:intercept-url pattern="/users/new" access="isAnonymous()"/>
        <security:intercept-url pattern="/users/create" access="isAnonymous()"/>
        <security:intercept-url pattern="/users/**" access="hasRole('ROLE_USER')"/>
        <security:form-login
                login-page="/auth/login"
                login-processing-url="/login/process"
                username-parameter="email"
                password-parameter="password"
                default-target-url="/users/index"
                always-use-default-target="true"
                authentication-failure-url="/auth/login?error=true"/>
        <!--<security:remember-me key="token" token-validity-seconds="2500000" remember-me-parameter="rem"/>-->
        <security:logout logout-url="/logout" logout-success-url="/auth/login"/>
    </security:http>

    <security:authentication-manager>
        <security:authentication-provider ref="authProvider"/>
    </security:authentication-manager>

    <bean id="authProvider" class="com.aidar.security.AuthProviderImpl"/>

</beans>